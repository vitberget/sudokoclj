(ns sudoku.solve
  (:require [clojure.set :as set]
            [sudoku.board :as board]
            [sudoku.check :as check]
            [ysera.test :refer [is is-not is=]]))

(defn solve
  [board]
  board)